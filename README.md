# LeetCode_Kotlin

Minimal Kotlin/Gradle project containing LeetCode task solutions.

Run locally:

```bash
cd LeetCode_Kotlin
./gradlew build    # generate wrapper first if needed: gradle wrapper
./gradlew run
./gradlew test 
```
